// Placeholder homepage - proves the RTL Hebrew layout renders correctly.
// The real homepage (LoRa explainer + distress-call flow diagram) is built
// in Phase 5.
export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-3xl font-semibold">מערכת דפיברילטורים ניידים</h1>
      <p className="max-w-md text-zinc-600 dark:text-zinc-400">
        תוכן העמוד הראשי ייבנה בשלב הבא.
      </p>
    </main>
  );
}
