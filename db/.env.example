# Same Supabase project as auth-server/.env and web/.env - this script
# is a one-off maintenance tool, not a deployed server, so it gets its
# own local .env rather than sharing one of theirs.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
